You are an expert frontend developer.

Build a fully functional Background Remover Web App using ONLY HTML, CSS, and JavaScript (no frameworks like React).

The app must allow users to upload an image and remove its background automatically using a real working method (preferably via an API like remove.bg or any free background removal API, or implement a client-side solution if possible).

========================
CORE FEATURES
========================
1. Image Upload
- Drag & drop area + file input button
- Accept JPG, PNG, WEBP
- Show preview immediately after upload

2. Background Removal
- A clear “Remove Background” button
- On click, process the image using:
  - remove.bg API OR any reliable free API
  - Handle API key via a config variable
- Show loading spinner while processing

3. Output Preview
- Show original image and processed image side-by-side
- Transparent background preview (use checkerboard pattern)

4. Download Option
- Download processed image as PNG (transparent background)

========================
UI/UX REQUIREMENTS
========================
- Clean, modern UI (minimalistic)
- Centered layout with responsive design
- Smooth animations (hover effects, transitions)
- Drag & drop highlight effect
- Loading spinner while processing
- Toast or alert messages for errors (API fail, invalid file, etc.)

========================
ADVANCED FEATURES (IMPORTANT)
========================
- Show file size and image resolution
- Allow re-upload / reset
- Dark mode toggle
- Progress indicator while processing
- Error handling (API errors, network issues)
- Limit file size (e.g., max 5MB)

========================
TECHNICAL REQUIREMENTS
========================
- Pure HTML, CSS, JS (no frameworks)
- Use Fetch API for API calls
- Use async/await
- Modular JS structure (separate functions)
- Clean and readable code
- Comment important sections

========================
BONUS (IF POSSIBLE)
========================
- Before/After slider comparison
- Option to change background color
- Local processing fallback (if API fails)

========================
OUTPUT FORMAT
========================
- Provide complete working code:
  - index.html
  - style.css
  - script.js
- Code should run directly in browser
- Include instructions for adding API key

========================
IMPORTANT
========================
- Do NOT just create UI — background removal must actually work
- Do NOT use placeholder logic
- Ensure everything is functional end-to-end


GqkQ6XNKMMBqYqaD2xPNiF8M